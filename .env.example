# Required
SOURCE_PDF_URL="https://example.com/path/to/report.pdf"
DROPBOX_ACCESS_TOKEN="replace-with-real-token"

# Optional
DROPBOX_TARGET_FOLDER="/Reports"
DB_PATH="data/app.sqlite"

# Optional (only if we add manual trigger later)
# ADMIN_TOKEN="some-random-string"
